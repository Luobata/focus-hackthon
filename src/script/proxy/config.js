export const domain = '';

export const domainHouse = '//house-sv-base.focus' + window.base + '.cn';

export const domainMp = '//mp-be-manage.focus' + window.env + '.cn';

export const domainLogin = '//common-ldap.focus' + window.env + '.cn';
